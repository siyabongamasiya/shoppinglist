import React from "react";
import Button from "../components/button";

export default function Homepage() {
  return (
    <div>
      <Button text="test" onClick={() => console.log("test")} style={{}} />
    </div>
  );
}
