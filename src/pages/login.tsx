import { useAppSelector, useAppDispatch } from "../../reduxHooks";
import { login } from "../features/userManagement";

export default function Login() {
  const dispatch = useAppDispatch();

  return (
    <div>
      
    </div>
  );
}
